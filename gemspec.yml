name: dm-chunked_query
version: 0.3.0
summary: Allows performing chunked queries with DataMapper.
description:
  Allows performing chunked queries on DataMapper Models or Collections.

license: MIT
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: https://github.com/postmodern/dm-chunked_query
has_yard: true

dependencies:
  dm-core: ~> 1.0
  dm-aggregates: ~> 1.0

development_dependencies:
  ore-tasks: ~> 0.4
  rspec: ~> 2.4
  yard: ~> 0.7
